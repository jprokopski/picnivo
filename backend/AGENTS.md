# Backend — AGENTS.md

## Commands

- Run: `dotnet run` (from `Picnivo.API/`)
- Build: `dotnet build` (from `Picnivo.API/`)
- Test: `dotnet test` (from `backend/`)
- Deploy (manual): `fly deploy` (from `backend/`)
- Logs: `fly logs --app picnivo`
- SSH: `fly ssh console --app picnivo`

## Local Database

Local Postgres runs via Supabase CLI on port 54322.

- Start: `supabase start -x realtime,storage-api,imgproxy,postgrest,postgres-meta,studio,edge-runtime,logflare,vector,supavisor`
- Stop: `supabase stop`
- Restore tools: `dotnet tool restore` (one-time after clone)
- Apply migrations: `dotnet ef database update --project Picnivo.API` (from `backend/`)
- Add migration: `dotnet ef migrations add <Name> --project Picnivo.API --output-dir Data/Migrations` (from `backend/`)

## Authentication

- Supabase Auth handles identity (email/password + Google OAuth)
- Backend validates JWTs via JWKS discovery at `Supabase:Authority` — no shared secret needed (ES256 asymmetric signing)
- Protected endpoints use `.RequireAuthorization()` — the `sub` claim carries the Supabase user ID
- `organizers` table auto-provisioned via `handle_new_user()` trigger on `auth.users` INSERT
- Production secrets: `Supabase__Authority`, `Frontend__Url` set via `fly secrets set`
- Local Google OAuth: set `GOOGLE_CLIENT_ID` and `GOOGLE_CLIENT_SECRET` in `backend/supabase/.env` (gitignored)

## Project Structure

```
backend/
├── Picnivo.API/
│   ├── Data/
│   │   ├── Configurations/      # EF Core entity configurations
│   │   ├── Migrations/
│   │   └── PicnivoDbContext.cs
│   ├── Features/
│   │   └── <FeatureName>/
│   │       └── <ActionName>/    # one folder per action
│   │           ├── <ActionName>.cs          # handler (static Handle method)
│   │           ├── <ActionName>Dtos.cs      # request/response DTOs (private to this action)
│   │           └── <ActionName>Endpoint.cs  # implements IEndpoint, owns route + auth
│   ├── EndpointExtensions.cs    # MapEndpoints() — auto-discovers IEndpoint via reflection
│   ├── IEndpoint.cs             # interface: void Map(WebApplication app)
│   └── Program.cs               # calls app.MapEndpoints(); nothing feature-specific
└── Picnivo.Tests/
    └── Features/
        └── <FeatureName>/
            └── <ActionName>/    # mirrors Features/ structure — one file per action
```

## Conventions

### Vertical slicing
- Each feature lives under `Features/<FeatureName>/`.
- Each action in a feature lives in its own subfolder `Features/<FeatureName>/<ActionName>/`.
- The action folder owns everything: handler, DTOs, validators, helpers, and endpoint. Do not share artifacts across action folders.

### Validation
- Use FluentValidation (`AbstractValidator<TRequest>`) for request validation.
- Validators are registered and invoked via `ValidationEndpointFilter` — wired automatically to all endpoints.

### Endpoint registration
- Each action exposes a class `<ActionName>Endpoint : IEndpoint` in its folder.

### Tests
- Integration tests mirror the `Features/<Name>/<ActionName>/` folder structure.
- Endpoint tests use the typed `PicnivoApiClient` (NSwag-generated, in `Picnivo.Tests.Client` namespace).
- The client is generated at build time from `Picnivo.API/Picnivo.API.json` (spec generated by `Microsoft.Extensions.ApiDescription.Server`).
- `ctx.ApiClient` — unauthenticated. `ctx.AuthedApiClient(organizerId)` — authenticated.
- After adding/changing endpoints: build regenerates the spec and client automatically.
- Use **Shouldly** for assertions (e.g. `result.ShouldBe(...)`, `response.ShouldNotBeNull()`).

### General
- Minimal API style (not controllers).
- No HTTPS redirection in app code — Fly.io terminates TLS at proxy.
- Health check at `/healthz` — keep it fast, no heavy dependencies.
- Secrets via `fly secrets set KEY=VALUE` — never in `fly.toml` or `appsettings`.
